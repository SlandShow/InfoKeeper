<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.test_apps.slandshow.infokeeper.MainActivity">

    <Button
        android:id="@+id/btnAddEntry"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignEnd="@+id/btnViewEntries"
        android:layout_alignParentTop="true"
        android:layout_alignStart="@+id/btnViewEntries"
        android:layout_marginTop="93dp"
        android:text="Add Entry" />

    <Button
        android:id="@+id/btnViewEntries"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/btnAddEntry"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="36dp"
        android:text="View all Entries" />

    <Button
        android:text="Generate Password"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="39dp"
        android:id="@+id/btnGeneratePass"
        android:layout_below="@+id/btnViewEntries"
        android:layout_alignStart="@+id/btnViewEntries"
        android:layout_alignEnd="@+id/btnViewEntries" />

    <Button
        android:id="@+id/btn_change_pass"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_marginBottom="32dp"
        android:layout_toStartOf="@+id/btnGeneratePass"
        android:text="Change Password" />

    <Button
        android:id="@+id/btn_off_pass"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignTop="@+id/btn_change_pass"
        android:layout_toEndOf="@+id/btnGeneratePass"
        android:text="Disable Password" />
</RelativeLayout>
